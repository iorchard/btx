BTX (Burrito Toolbox)
========================

BTX is a Toolbox image for Burrito platform.

Build (Optional)
-------------------

The default version of clients is openstack yoga release.

Build the image.::

   $ ./build.sh

